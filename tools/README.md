# Foundation tools

The supported interface is `python -m spec_viewer`. Implementation lives under `src/spec_viewer`;
these directories reserve the responsibility boundaries for future standalone tools.
