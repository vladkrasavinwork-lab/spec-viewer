# Repository examples

Only explicitly classified public synthetic or sanitized examples may be tracked. Every example
directory requires a valid `classification.yaml`. Never copy a customer workspace here.
