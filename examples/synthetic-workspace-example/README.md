# Synthetic workspace example

This tracked directory demonstrates a minimal workspace shape with entirely fictional data. Real
projects must stay under ignored `workspaces/<slug>_private/`; never copy a real workspace here.
